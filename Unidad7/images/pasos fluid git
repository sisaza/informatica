1. Crear nuevo Branch
    git checkout -b 'scudriz'

2. Realizar modificaciones

3. Hacer la integración local con Docker
    gitlab-runner exec docker --env DOCKER_USER=[username] --env DOCKER_PASS=[password] --docker-privileged builder
    gitlab-runner exec docker --env DOCKER_USER=[username] --env DOCKER_PASS=[password] --docker-privileged test

4. Añadir los cambios al stage
    git add .

5. Realizar commit
    git commit 

6. Hacer el PUSH a la rama remota
   git push origin scudriz

7. Ir a página web de gitlab y realizar el Merge Request (MR)

-----------------------------------
Comandos útiles:

- Eliminar una rama: git branch -d scudriz
- Eliminar último commit: git reset --HARD HEAD~1
NOTA: Se eliminan hasta los cambios. Todo queda limpio 
